{
  "name": "cr-private-server",
  "private": true,
  "version": "0.1.0",
  "description": "Browser-based server-authoritative royale battle private server.",
  "scripts": {
    "test": "npm run test:docs && npm run test:web && npm run test:web:build && npm run test:dotnet && npm run test:e2e",
    "test:docs": "node scripts/validate-docs.mjs",
    "test:web": "node scripts/validate-web.mjs",
    "test:web:build": "npm --prefix src/Game.Web run build",
    "test:dotnet": "dotnet test CRPrivateServer.sln",
    "test:e2e": "node scripts/prepare-e2e.mjs && node scripts/run-e2e.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
